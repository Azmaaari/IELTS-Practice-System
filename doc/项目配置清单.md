# 项目配置清单

> **用途**: 确保所有配置文件正确创建和配置  
> **使用方式**: 按顺序检查每个配置项，完成后打✅

---

## ✅ 第一步：执行初始化脚本

```bash
cd /Users/armazi/Desktop/code/IELTS-Practice-System
chmod +x scripts/init-project.sh
./scripts/init-project.sh
```

**预期结果**:
- ✅ 所有依赖已安装
- ✅ 目录结构已创建
- ✅ 基础配置文件已生成

---

## ✅ 第二步：配置核心文件

### 2.1 配置 vite.config.ts

**位置**: `vite.config.ts`

**内容**: 参考 `doc/技术设计方案.md` 第1.3节

**需要配置**:
- ✅ Vue插件
- ✅ PWA插件
- ✅ 路径别名（@, @components, @views等）
- ✅ 构建优化（代码分割）

**验证**: 运行 `npm run dev` 不报错

---

### 2.2 配置 tailwind.config.js

**位置**: `tailwind.config.js`

**内容**: 参考 `doc/技术设计方案.md` 第2.1节

**需要配置**:
- ✅ content 路径
- ✅ 主题扩展（颜色、字体、间距）
- ✅ 插件（forms, typography）

**验证**: Tailwind类名在组件中正常工作

---

### 2.3 配置 tsconfig.json

**位置**: `tsconfig.json`

**需要添加的配置**:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "preserve",

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    /* Path Aliases */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@components/*": ["./src/components/*"],
      "@views/*": ["./src/views/*"],
      "@stores/*": ["./src/stores/*"],
      "@composables/*": ["./src/composables/*"],
      "@types/*": ["./src/types/*"],
      "@utils/*": ["./src/utils/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**验证**: 路径别名导入正常工作

---

### 2.4 创建 postcss.config.js

**位置**: `postcss.config.js`

**内容**:

```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

---

## ✅ 第三步：创建样式文件

### 3.1 主样式文件

**位置**: `src/styles/index.css`

**内容**: 参考 `doc/技术设计方案.md` 第2.2节

**包含**:
- ✅ Tailwind导入
- ✅ 基础样式
- ✅ 自定义组件样式
- ✅ 实用工具类

---

### 3.2 在 main.ts 中导入样式

**位置**: `src/main.ts`

**添加**:

```typescript
import './styles/index.css'
```

---

## ✅ 第四步：创建类型定义

### 4.1 考试类型

**位置**: `src/types/exam.ts`

**内容**: 参考 `doc/技术设计方案.md` 第6.1节

**包含的类型**:
- ✅ Manifest
- ✅ ManifestItem
- ✅ Passage
- ✅ Paragraph
- ✅ Question (及所有题型的content接口)
- ✅ Metadata
- ✅ ExamData
- ✅ ChunkCache

---

### 4.2 练习类型

**位置**: `src/types/practice.ts`

**内容**: 参考 `doc/技术设计方案.md` 第6.2节

**包含的类型**:
- ✅ SessionType
- ✅ UserAnswer
- ✅ PracticeSession (Single和FullTest)
- ✅ Highlight
- ✅ GradingResult

---

### 4.3 历史记录类型

**位置**: `src/types/history.ts`

**内容**: 参考 `doc/技术设计方案.md` 第6.3节

**包含的类型**:
- ✅ SessionRecord

---

## ✅ 第五步：配置数据库

### 5.1 创建 IndexedDB 配置

**位置**: `src/db/index.ts`

**内容**: 参考 `doc/技术设计方案.md` 第7节

**包含**:
- ✅ Dexie数据库类
- ✅ sessions表定义
- ✅ 导出db实例

---

## ✅ 第六步：配置路由

### 6.1 创建路由配置

**位置**: `src/router/index.ts`

**内容**: 参考 `doc/技术设计方案.md` 第3.2节

**包含的路由**:
- ✅ / (首页)
- ✅ /search (搜索)
- ✅ /practice/:id (单篇练习)
- ✅ /practice/full-test/confirm (套题确认)
- ✅ /practice/full-test (套题练习)
- ✅ /history (历史)
- ✅ /history/:sessionId (历史详情)
- ✅ /stats (统计)
- ✅ /settings (设置)

---

### 6.2 在 main.ts 中注册路由

**位置**: `src/main.ts`

**添加**:

```typescript
import router from './router'

app.use(router)
```

---

## ✅ 第七步：创建 Pinia Stores

### 7.1 examStore

**位置**: `src/stores/examStore.ts`

**内容**: 参考 `doc/技术设计方案.md` 第4.2节

---

### 7.2 practiceStore

**位置**: `src/stores/practiceStore.ts`

**内容**: 参考 `doc/技术设计方案.md` 第4.2节

**注意**: 需要安装 pinia-plugin-persistedstate

```bash
npm install pinia-plugin-persistedstate
```

---

### 7.3 historyStore

**位置**: `src/stores/historyStore.ts`

**内容**: 参考 `doc/技术设计方案.md` 第4.2节

---

### 7.4 userStore

**位置**: `src/stores/userStore.ts`

**内容**: 参考 `doc/技术设计方案.md` 第4.2节

---

### 7.5 统一导出

**位置**: `src/stores/index.ts`

**内容**:

```typescript
export { useExamStore } from './examStore'
export { usePracticeStore } from './practiceStore'
export { useHistoryStore } from './historyStore'
export { useUserStore } from './userStore'
```

---

### 7.6 在 main.ts 中注册 Pinia

**位置**: `src/main.ts`

**添加**:

```typescript
import { createPinia } from 'pinia'
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'

const pinia = createPinia()
pinia.use(piniaPluginPersistedstate)

app.use(pinia)
```

---

## ✅ 第八步：更新 main.ts

**位置**: `src/main.ts`

**最终内容**:

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import piniaPluginPersistedstate from 'pinia-plugin-persistedstate'
import router from './router'
import App from './App.vue'
import './styles/index.css'

const app = createApp(App)

const pinia = createPinia()
pinia.use(piniaPluginPersistedstate)

app.use(pinia)
app.use(router)

app.mount('#app')
```

---

## ✅ 第九步：更新 App.vue

**位置**: `src/App.vue`

**内容**:

```vue
<template>
  <div id="app" class="min-h-screen bg-gray-50">
    <router-view />
  </div>
</template>

<script setup lang="ts">
import { onMounted } from 'vue'
import { useExamStore } from '@stores/examStore'

const examStore = useExamStore()

onMounted(async () => {
  // 加载manifest
  try {
    await examStore.loadManifest()
  } catch (error) {
    console.error('Failed to load manifest:', error)
  }
})
</script>
```

---

## ✅ 第十步：创建占位页面

创建基础的页面组件，确保路由可以正常工作：

### 10.1 首页

**位置**: `src/views/HomeView.vue`

```vue
<template>
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-4xl font-bold text-primary-600 mb-4">
      IELTS 阅读练习平台
    </h1>
    <p class="text-gray-600">
      首页开发中...
    </p>
  </div>
</template>
```

### 10.2 其他页面

同样创建占位组件：
- ✅ SearchView.vue
- ✅ HistoryView.vue
- ✅ StatsView.vue
- ✅ SettingsView.vue
- ✅ NotFoundView.vue
- ✅ practice/PracticeLayout.vue
- ✅ practice/SinglePracticeView.vue
- ✅ practice/FullTestView.vue
- ✅ practice/FullTestConfirmView.vue

---

## ✅ 第十一步：验证配置

### 11.1 启动开发服务器

```bash
npm run dev
```

**预期结果**:
- ✅ 服务器正常启动
- ✅ 无TypeScript错误
- ✅ 无ESLint错误

---

### 11.2 检查路由

访问以下路由，确保正常显示：
- ✅ http://localhost:5173/
- ✅ http://localhost:5173/search
- ✅ http://localhost:5173/history
- ✅ http://localhost:5173/settings

---

### 11.3 检查Tailwind

在任意组件中添加Tailwind类名，确保样式生效：

```vue
<div class="bg-primary-500 text-white p-4 rounded-lg">
  Tailwind 正常工作
</div>
```

---

### 11.4 检查Store

在浏览器控制台执行：

```javascript
// 应该能访问到stores
window.__PINIA__
```

---

## ✅ 第十二步：提交初始代码

```bash
git add .
git commit -m "feat: 项目初始化和基础配置"
git push origin feature/armazi
```

---

## 📋 配置完成检查清单

- [ ] 所有依赖已安装
- [ ] vite.config.ts 已配置
- [ ] tailwind.config.js 已配置
- [ ] tsconfig.json 已配置
- [ ] 样式文件已创建
- [ ] 类型定义已创建
- [ ] 数据库已配置
- [ ] 路由已配置
- [ ] Stores已创建
- [ ] main.ts 已更新
- [ ] App.vue 已更新
- [ ] 占位页面已创建
- [ ] 开发服务器正常运行
- [ ] 路由导航正常
- [ ] Tailwind样式正常
- [ ] Pinia stores正常工作

---

## 🎯 下一步开发计划

配置完成后，可以开始实际功能开发：

1. **数据加载系统**
   - 创建 useExamLoader composable
   - 实现 manifest 加载
   - 实现 chunk 动态加载

2. **搜索功能**
   - 创建 useSearch composable
   - 实现 Fuse.js 搜索
   - 创建搜索页面UI

3. **单篇练习**
   - 创建练习页面布局
   - 实现文章渲染
   - 创建题型组件

4. **套题功能**
   - 实现随机组题
   - 创建套题确认页面
   - 创建套题练习页面

详见 `doc/技术设计方案.md` 第8节开发优先级。

---

**最后更新**: 2025-10-19

